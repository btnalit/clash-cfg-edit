# 服务端口
PORT=3000

# 是否启用认证（true/false）
# 生产环境强烈建议设置为 true
AUTH_ENABLED=false

# 登录用户名（当AUTH_ENABLED=true时生效）
# 请使用强密码！
AUTH_USERNAME=admin

# 登录密码（当AUTH_ENABLED=true时生效）
# 请使用强密码！至少12位，包含大小写字母、数字和特殊字符
AUTH_PASSWORD=admin

# 允许的跨域源（逗号分隔，留空表示允许所有）
# 生产环境建议配置具体域名，例如：https://yourdomain.com
ALLOWED_ORIGINS=

# 生产环境标识
NODE_ENV=production
